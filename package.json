{
  "name": "leaflet-image-poster",
  "version": "0.1.0",
  "description": "Export leaflet map to a big image file using leaflet-image.",
  "scripts": {
    "start": "browserify main.js > bundle.js && http-server -a localhost",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "rzoller/leaflet-image-poster",
  "author": "Roman Zoller",
  "license": "MIT",
  "devDependencies": {
    "browserify": "^16.2.2",
    "http-server": "^0.11.1"
  },
  "dependencies": {
    "blueimp-canvas-to-blob": "^3.14.0",
    "node-safe-filesaver": "^0.1.0"
  }
}
